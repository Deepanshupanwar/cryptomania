{
    "buildCommand": "cd front-end && yarn install && yarn build",
    "outputDirectory": "front-end/build",
    "framework": "vite",
    "rewrites": [
      {
        "source": "/back-end/(.*)",
        "destination": "/back-end/server.js"
      },
      {"source": "/(.*)", "destination": "/"}
    ]
  }